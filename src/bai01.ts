let userName: string;
let age: number;
let job: string;

const printUser = (userName: string, age: number, job: string) => {
  alert(`UserName: ${userName}\nage: ${age}\njob: ${job}`);
};

printUser("minh", 15, "it");
